/**
 * XPulse authentication providers.
 *
 * There are intentionally no social providers here.
 * Application authentication is performed exclusively by Solana wallet
 * signature (SIWS-style challenge + Ed25519 verification).
 */
export const GROK_PROVIDERS = [] as const;
